import React from 'react';
import CartList from '../components/CartList';

const CartPage = () => {
    return (
        <div>
            <CartList />
        </div>
    );
};

export default CartPage;
